# OpenBy 响应速度诊断与底层优化计划

日期：2026-09-06。状态：核心优化已实现，68 项回归通过，Release 1.1（build 2）已打包、签名并进行真实文件转发验证。

下面的诊断和实施顺序保留为优化前记录；最新实施结果、测量数据和验证边界见文末。

目标：优化所有受管理文件类型共用的打开链路。采用后台常驻作为主要方案，让用户再次打开文件时复用已启动的路由进程与内存配置。

**已确认的事实**

| 发现 | 代码证据 | 对响应速度的影响 |
| --- | --- | --- |
| 当前进程不是后台常驻 | `OpenRequestCoordinator.swift:36,79-93`：转发完成后空闲 3 秒退出；`AppDelegate.swift:79-82`：无在途请求时关闭设置窗口也退出 | 间隔超过保活时间的请求需要重新启动 OpenBy、读取配置和初始化路由服务。冷启动是优先测量和优化的环节，但本轮尚未测出它贡献了多少毫秒。 |
| 后续批次被全局阻塞 | `OpenRequestCoordinator.swift:67,96-115,126-143`：`isFlushing` 阻止下一批处理，直到当前批次全部回调完成 | PDF Expert 的请求回调慢时，随后发往预览的请求也可能等待；同样影响其他文件类型和应用。 |
| 目标应用定位没有进程内缓存 | `ApplicationResolver.swift:41-53`：每个目标组重新通过 NSWorkspace 查询应用地址 | 热请求仍包含系统查询；`lastKnownPath` 只是查询失败后的兜底，并非优先使用的缓存。实际开销需分段计时确认。 |
| 类型解析存在重复工作 | `RuleEngine.swift:44-80`：每个文件构造 UTType，并在 handler 循环中解析 handler 类型 | 可以预编译和缓存；需要保留现有 handler 顺序、UTI 继承及扩展名兼容行为。 |
| 转发前处理主要在主线程 | `AppDelegate.swift:20-23`；整个 `OpenRequestCoordinator` 标记为 `@MainActor` | 配置读取、类型判定、应用定位与 UI 共用主线程。设置页中的同步自检也可能影响接收和处理请求。 |
| 真实打开回调的线程边界不完整 | `WorkspaceOpening.swift:34-38` 原样转出系统回调；`OpenRequestCoordinator.swift:131-135` 直接更新错误列表 | Apple 明确系统回调在并发队列执行；错误状态应统一回到所属 actor。这是正确性隐患，不能认定为本次慢的直接原因。 |
| 缺少端到端性能记录 | 现有 Benchmark 只计规则解析，生产链路没有阶段计时 | 无法从现有日志区分进程启动、规则解析、应用定位、系统转发与目标窗口显示。 |

真实打开 API 的线程约定见 [Apple NSWorkspace 文档](https://developer.apple.com/documentation/appkit/nsworkspace/open(_:withapplicationat:configuration:completionhandler:))：可以从任意线程调用，completion 在并发队列执行。

**历史问题与当前版本的区别**

- Git 提交 `4c19259` 的 `handle → resetKeepAlive → 3 秒后 flush` 会在转发之前固定等待，并在连续请求到来时重置等待。这足以造成明显延迟。
- 当前工作区已修改为收到文件立即 `flush`，在完成后才开始 3 秒退出计时。该修正不是本次新增。
- `dist/OpenBy.app` 的二进制符号包含 `cancelIdleTermination`、`scheduleIdleTermination` 和 `terminationGeneration`，与修正后的生命周期实现一致。
- 尚未确认 Finder 实际启动的是哪一份 OpenBy；因此不能把历史版本的固定等待直接认定为当前正在使用版本的原因。
- `AppDelegate.swift:58` 的 0.8 秒延迟只用于普通启动时显示设置窗口，不会阻止已收到的文件请求转发。
- `AssociationService` 的关联验证轮询用于设置默认应用，不在正常打开文件的调用链上。

**本次测量与边界**

当前用户配置为 750 字节、一个 PDF handler、一条文件夹规则，默认目标为预览，规则目标为 PDF Expert。没有读取用户 PDF 的内容。

以下为当前源码的 Release 基准，使用正常系统访问环境，每组预热 100 次后计时 2,000 次：

| 规则数 | 平均单次路由耗时 |
| ---: | ---: |
| 1 | 0.0066 ms |
| 1,000 | 0.0484 ms |

命令：`swift run -c release OpenByBenchmark --rules <数量> --iterations 2000`。

这些数字只描述预热后的 `resolveDecision` 平均调用耗时，不含进程启动、首次 UTI 查询、配置加载、应用定位、NSWorkspace 转发和文档窗口显示，也不是 P95。现有基准未断言命中目标，实施第一步应补上结果校验，并单独报告首次调用。沙箱环境下的系统类型查询可能与真实应用不同，因此不采用沙箱内的大规则数结果作比较。

结论：现有测量不支持“文件夹规则扫描导致秒级延迟”。优先改造生命周期和请求调度，同时建立完整测量链路；目标应用本身的启动或文档加载耗时仍需用直开对照排除。

**拟采用的结构**

`Finder 文件事件 → 轻量接收入口 → 后台路由服务（配置快照、类型缓存、应用地址缓存）→ 按目标应用独立调度 → 显式 NSWorkspace.open`

采用单进程后台常驻，无需为此引入额外 helper 或 XPC 服务。主线程负责 AppKit 生命周期、设置窗口和菜单；路由服务串行管理自身状态，系统打开请求异步进行。后台路由服务不等待目标应用打开完成才接受下一次请求。

**实施顺序与验收**

1. **建立可比较的基线，确认实际运行版本。**

   在诊断中显示应用绝对路径、版本、构建标识和 PID；核对 Finder 实际启动路径。为每个请求记录单调时钟时间点：进程入口、收到文件事件、配置可用、匹配完成、应用地址可用、发出系统打开请求、系统回调完成。默认使用有容量上限的内存记录，按需启用 signpost，不在正常路由中同步写磁盘。

   基准补齐命中结果断言、首次调用和未命中规则走 fallback 的情况。保存优化前结果。将“发出请求”“系统回调完成”“窗口可见”分开；不能把系统回调时间当作文档已显示的时间。

2. **改为后台常驻，移除反复冷启动。**

   修改 `AppBootstrap`、`AppDelegate`、`OpenRequestCoordinator` 及应用声明：文件转发结束后继续驻留，关闭设置窗口后继续驻留，后台不占 Dock；增加菜单栏的设置和退出入口。保留用户主动退出能力，再次通过文件唤起时可以正常启动并路由。

   提供“登录时启动”设置，使用系统支持的登录项机制，区分登录后台启动与用户主动打开应用，避免登录时弹设置窗口。采用事件驱动的空闲等待，不增加周期轮询或持续保持系统唤醒。登录项具体实现需核对当前 SDK 和 Apple 文档。

   验收：间隔 10 秒、1 分钟、5 分钟重复打开文件，PID 保持不变；关闭设置窗口不终止路由服务；登录启动无设置窗口；主动退出后可以重新按文件启动。

3. **消除不同目标应用之间的等待。**

   把全局 `isFlushing` 屏障替换为按目标应用管理的在途状态。同一目标保持有序并适量合并，不同目标可独立发出请求。单文件请求不增加人为合并延迟。每批请求持有接收时的配置快照，使配置编辑不会改变已在途请求的 fallback。

   去重只覆盖明确的在途请求，结束后允许再次打开；设置有界的超时清理和过期回调识别，避免未返回回调永久占用状态。超时不等于文件未打开，不自动盲目重发，以免重复打开。所有完成和错误状态更新回到所属 actor。

   验收：通过可控延迟 opener 让目标 A 暂不回调，目标 B 仍立即收到请求；目标 A 最终失败、超时或晚回调均不影响 B；不会丢文件、重复转发或错误退出。现有“首批完成前暂存下一批”的测试应改为验证新的按目标调度行为。

4. **让日常路由复用内存结果。**

   `ApplicationResolver` 增加共享的应用地址缓存，首次定位并校验身份，后续复用；配置更新、明确的目标失效及缓存过期触发更新。缓存失效重新定位仍须校验 bundle ID，保留防递归校验；不能简单信任旧路径。找不到应用的结果使用短期或事件驱动失效，避免安装应用后仍永久判定缺失。

   `RuleEngine` 在配置加载或保存时预编译 handler 类型和扩展名，按规范化扩展名缓存完整的 handler 匹配结果，保持原先匹配优先级。不要用“扩展名直达”越过排在前面的通用 handler。配置变更清空相关缓存；缓存设容量上限。配置继续使用内存快照；若支持外部编辑，用目录事件监听处理原子替换，不轮询读文件。

   保留原有路径规则语义和线性匹配；去除重复标准化时需覆盖 Unicode、大小写、路径边界和符号链接既定行为。将慢速配置读取、应用解析和设置自检移出主线程；预热只做必要元数据解析，不提前启动目标应用，也不阻塞已经到来的首个请求。

   验收：同类型、同目标的热请求不重复定位应用；配置保存后下一次请求使用新规则；应用移动、卸载、重新安装后的恢复正确；常驻运行缓存不持续增长。

5. **跨文件类型验证并交付 Release 构建。**

   测试 PDF、PNG/JPEG、TXT、DOCX 等机器上有可用目标应用的类型，覆盖命中规则与 fallback。对比直接用目标应用打开和通过 OpenBy 打开；分别覆盖 OpenBy 冷/热、目标应用冷/热、连续多目标、多文件批量、设置窗口打开、睡眠唤醒及应用失效。

   运行原有功能测试和新增调度、缓存、生命周期测试。性能场景每组至少 100 次，报告中位数、P95 和最大值，保持文档与目标应用状态可比。以常驻后台、目标已运行作为主要日常验收场景；对同一文档重复打开需关闭或轮换测试文档，避免把聚焦已有窗口当作重新打开。

   更新开发文档、发布说明和构建标识，使交付包可以与源码对应。安装位置保持稳定，验证系统关联指向新构建，不以清空 Launch Services 数据库作为常规修复。

**性能验收目标（待实测，非已达成）**

| 指标 | 目标 |
| --- | --- |
| 常驻且缓存已就绪：收到 URL 到发出目标应用打开请求 | P95 < 10 ms |
| OpenBy 冷启动：进程入口到发出目标应用打开请求 | P95 < 150 ms；另外单列 Finder 发起到进程入口的系统启动时间 |
| 相同文档、相同目标运行状态下的可见响应 | 相比直开，额外延迟目标 P95 < 100 ms，需具备可靠的窗口可见测量手段 |
| 目标 A 回调慢时发往目标 B 的热请求 | 不等待 A，满足热请求转发指标 |
| 空闲资源占用 | CPU 接近 0%，无周期轮询；内存先以 < 30 MB 为目标并报告实测 footprint |

初次诊断阶段仅新增计划、运行基准；用户随后授权实施，改造结果如下。


**实施结果（2026-09-06）**

- 单进程后台常驻，LSUIElement + accessory 不占 Dock；关闭设置窗口不退出。菜单栏支持设置、退出和基于 SMAppService 的登录启动；未自动更改用户登录项。
- 配置加载和预编译在后台执行；协调器状态由独立串行队列管理。应用定位和实际打开按目标并行，同目标保持顺序，已排队的请求保留原配置 fallback。
- 30 秒超时清理、在途去重、迟到回调 ID 校验、缺失目标批量降级及错误后缓存失效均实现并有回归覆盖。超时不会自动重发文档。
- 类型缓存上限 256，应用缓存上限 128；有效期 5 分钟，按配置和应用生命周期事件失效。应用缺失不永久缓存；后备路径验证 bundle ID。仍使用既定规则语义，不添加 PDF 特例。
- 诊断自检移到后台，显示版本、构建日期、实际应用路径、PID、初始化耗时与最近 100 组请求的分段计时。错误最多 20 条；正常路由不写日志文件。
- `./scripts/build-app.sh --no-register` 完成打包，`codesign --verify --strict` 和 plist 校验通过。保留原有配置和默认关联；旧构建备份于 `.build/before-resident/OpenBy.app`。

**实测记录**

Release，1,000 条规则，每组 2,000 次，基准逐次断言实际路由结果：

| 测量范围 | 平均 | P95 | 最大 |
| --- | ---: | ---: | ---: |
| 热匹配最后一条规则 | 0.0282 ms | 0.0416 ms | 0.0979 ms |
| 热 fallback | 0.0250 ms | 0.0299 ms | 0.0657 ms |
| 收到请求 → FileOpening 调用（真实应用定位缓存、假打开） | 0.0506 ms | 0.0600 ms | 0.1505 ms |

首次转发（真实应用定位、假打开）3.1316 ms；规则引擎初始化 13.014 ms。上述测量不包含文档显示时间，不能据此宣称可见响应 P95 已达标。

真实应用验证使用新生成的 `/private/tmp/OpenBy-performance-smoke.pdf`，在 Finder 的“打开方式 → 其他”中明确选择 `dist/OpenBy.app`，没有勾选“始终以此方式打开”。预览成功显示测试文本；新版诊断记录收到事件到发出系统打开请求 0.45 ms，其中路由 0.17 ms、排队 0.08 ms、应用定位 0.19 ms，状态为“系统已确认”。这是单次真实样本，不是 P95。该进程入口到服务就绪为 100.15 ms，PID 15921 连续运行超过 15 分钟。

68 项回归全部通过，包含 PDF/PNG/JPEG/TXT/DOCX 类型缓存、通用 handler 优先级、不同应用独立调度、慢定位、超时/迟到回调、重复打开、配置更新、fallback、防递归、缓存失效与有界诊断记录。界面实测确认菜单栏设置/退出/登录项入口和诊断布局可用。

资源采样：打开设置和诊断后，空闲 CPU 0.0%，RSS 96,752 KiB（约 94.5 MiB）。RSS 不是物理 footprint；不能据此宣称原定 < 30 MB 目标已达成。后台驻留减少冷启动，但会保留 AppKit 和已加载 UI 的内存。

验证边界：尚未实际注销登录、睡眠唤醒或做跨应用文档可见时间的百次统计；登录启动代码与系统状态接口已接入，实际登录验证需在用户方便时进行。没有为外部直接编辑 JSON 添加监听，设置界面保存后立即更新路由。

现场发现系统登记了多份同名 Debug 包，且当前 PDF 默认应用是预览，OpenBy 界面显示“尚未启用”。测试采用显式打开新版，未改变默认关联。需要自动按规则分流时，应在新版设置中点击“启用自动打开”，并响应 macOS 的确认。


**最终交付复核（2026-09-07）**

- 关闭设置和诊断窗口后，PID 15921 在次日复核时仍运行，累计 13 小时 32 分钟；CPU 0.0%，RSS 59,744 KiB（约 58.3 MiB）。确认窗口关闭不会导致自动退出。
- 为 NSApplication 委托增加显式生命周期保留；最终源码重新构建，68 项回归再次全部通过。
- 桌面同步服务会在运行/访问裸应用包时重新附加 FinderInfo。构建脚本改为在本地临时目录完成组装、签名和严格校验，再输出应用和不含扩展属性的 ZIP，避免签名期间与同步服务竞争。
- 最终发布包：`dist/OpenBy-1.1.zip`，260,004 字节，版本 1.1 / build 2，构建时间 2026-09-07T02:27:30Z。
- 将 ZIP 独立解包到 `/private/tmp` 后，再次通过 `codesign --verify --strict --verbose=2`，并验证 ZIP 完整性、版本及 LSUIElement 声明。
- SHA-256：`94967139a15853c2094e8eb8e07f17fbbd618cebd7125d1c5c6ff71d1ad3b6a7`。

使用时退出旧 OpenBy，将发布包解压到稳定的本地应用目录后启动；系统中有多份同名 Debug 构建，应以诊断页显示的实际应用路径和构建时间核对版本。若需要按规则自动分流，在新版中确认对应文件类型的“自动打开”已启用。
